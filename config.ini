[Utility function]
# implemented metrics: f_b, jaccard, tss, deltaSR1, deltaSR2 #
metric = f_b

# list of parameters for the metric (e.g. beta for f_beta) #
param = 1


[File paths]
# case study selection (1: GeoLifeCLEF, 2: RLS, 3: American Birds) #
case_study = 2 

#base data path #
data_path = data/

[Experiment]
# use train for calibration #
train_calib = False

# predict on calibration set instead
predict_cal = False

# percentage of testset used for calibration if not train calib #
prob_val = 0.2  

# run in quadratic complexity if the metric allowed it to # -> not implemented
run_quad = False

# run only MaxExp method #
only_maxexp = False

# transpose species and samples (macro-averaged version)#
transpose = False

